import styled from 'styled-components';

const Wrapper = styled.button`
  background-color: wheat;
`;

const Button = () => {
  return <Wrapper>I am a button, click me if you dare</Wrapper>;
};
export default Button;
